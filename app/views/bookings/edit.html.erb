<div class="container">
  <div class="update" style="text-align: center">
    <h2>Modifier les dates</h2>
    <%= image_tag "calendrier.png", height: 200 %>
<!-- Flatpicker shared yield  houses/show.html.erb-->
    <div class="form-wrapper" style="width: 400px; margin: 0 auto;">
      <%= simple_form_for [@house, @booking] do |f| %>

        <%= f.input :start_on, as: :string, required: false, input_html: {class: "datepicker"} %>
        <%= f.input :end_on, as: :string, required: false, input_html: {class: "datepicker"} %>
        <%= f.button :submit, "Update", class: "btn btn-danger btn-block" %>
      <% end %>
    </div>

<!-- datepicker -->
   <!--  <%= simple_form_for [@house, @booking] do |f| %>
      <div class="form-inputs col-xs-8 col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3" >
        <%= f.input :start_on %>
        <%= f.input :end_on %>
      </div>

      <div class="form-actions col-xs-8 col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3" >
        <%= f.submit "Update", class: "btn btn-danger btn-block" %>
      </div>
    <% end %> -->


  </div>
</div>


